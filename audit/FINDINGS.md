# Audit findings log

Running log for the PLAN_AUDIT production-readiness audit. Every confirmed or suspected
issue lands here; phase reports live in `audit/reports/`.

**Severity** (PLAN_AUDIT §0.4): P0 crash/data-loss/security/store-rejection · P1 broken
feature on a real path · P2 degraded UX / edge case / visible perf · P3 polish & debt.
Items marked **lead** are pre-seeded from history/docs and still need in-phase
confirmation (PLAN_AUDIT §0.2 — no speculative fixes).

**Status**: open → fixed / wontfix (with justification). `Commit` fills when fixed.

| ID | Phase | Location | Sev | Summary | Status | Commit |
|---|---|---|---|---|---|---|
| F-001 | 0 | patches/expo-audio+56.0.12.patch | **P1** | `npm install` postinstall failed: patch-package could not apply the expo-audio patch. Root cause: `core.autocrlf=true` (Windows-era config) checks patches out with CRLF on this Mac, so hunk context never matches the LF node_modules sources; node_modules also held a stale half-patched state (old patch applied to 5 files, the newer AudioModule/BaseAudioPlayer audio-focus hunks never landed). Any fresh install / CI / EAS build would silently compile **unpatched** expo-audio → no lock-screen prev/next, no notification deep-link, no scrubber duration, no auto-advance focus fixes. Fixed: `.gitattributes` pins `*.patch -text`, patches re-checked-out LF, node_modules restored to pristine+patch (verified byte-identical, apply idempotent). | fixed | `8f54bbf` |
| F-002 | 0→2 | (infrastructure) | **P1** | No staging Supabase project existed. **Resolved 2026-07-15**: created `Almahajjah-staging` (ref `xjtpwcwotuflomqigzfa`, same org/region as production) via the Management API — owner paused the unrelated `Tareeg_DB` to free the free-plan slot (unpause after the audit). Full chain 0001–0090 (incl. unmerged 0088/0089 from `audit/phase-2-backend`) applied; `supabase_migrations.schema_migrations` backfilled (0001–0090) so the CLI works; verified 33 tables / 116 functions / 48 policies. Credentials in untracked `.env.staging.local` (URL, anon key, db password). Unblocks Phase 2 live verification + Phase 3/4/7/8 staged flows. Owner: **revoke the access token used** when the audit ends, and unpause Tareeg_DB. | fixed | n/a (infra) |
| F-003 | 0→1,5 | package.json | P2 | expo-doctor: react-native-gesture-handler **3.0.2** installed where SDK 56 expects ~2.31.1 (major mismatch). Bumped deliberately in `fbb61c1` ("ios", 2026-07-09) but the lockfile was never synced (Phase 0's npm install reconciled it). Gesture surfaces (player swipe-dismiss, sheets, drag scrubber) must be verified against v3 semantics in Phases 1/5, or the version aligned. | open | — |
| F-004 | 0→13 | package.json | P3 | expo-doctor patch-level lags: expo 56.0.14→.15, expo-asset .18→.19, expo-notifications .19→.20, expo-router 56.2.13→.14. Review changelogs and bump in a quiet phase (`npx expo install --check`). | open | — |
| F-005 | 0→13 | package-lock.json | P3 | `npm audit`: 11 moderate vulnerabilities (transitive). Enumerate, check none ships in the client bundle, triage in the Phase 13 release check. | open | — |
| F-006 | 0→11 | repo-wide | P3 | Entire working tree is CRLF on disk (autocrlf=true legacy checkout). Tools tolerate it; only patches actually broke (F-001, fixed). Consider repo-wide `.gitattributes` text normalization + `core.autocrlf=input` as a deliberate cleanup — big inert diff, do it in a quiet moment, not mid-audit. | open | — |
| F-007 | 0 | (dev environment) | P3 | Stale `expo start --web` from before this session held port 8081 (recurrence of the GLITCH_LOG #18 stray-Metro pattern; that one broke a release build via metro-cache contention). Killed. Habit: check `lsof -i :8081` before builds. | fixed | n/a (process) |
| F-008 | 0 | CLAUDE.md | P2 | Doc rot: "out of scope" section still lists quizzes, attachments-as-a-system, notifications, streaks/badges/journey, study companion, onboarding as NOT built — all are fully shipped (V2→V17). Completion threshold documented as 90% vs `COMPLETE_THRESHOLD = 0.95` (V17). MVP list describes the app as pre-quiz MVP. Later phases would audit against a false spec. Rewritten to match reality. | fixed | `8f54bbf` |
| F-009 | 0 | app/(student)/profile.tsx:1 | P3 | Stale header comment: "minimal account page … No streaks/badges/journey — explicitly deferred" — the screen is actually the full settings hub (journey link, downloads, about, rate/share, privacy, tour replay, notification prefs, playback settings, bubble consent, buddy settings, sign-out, delete-account). Comment rewritten. | fixed | `8f54bbf` |
| F-010 | 0→1 | app/_layout.tsx:137 | **P1 lead** | Acknowledged TODO: Android has no native RTL enforcement (`MainApplication.kt` not mirrored to `AppDelegate.swift`'s fix); first launch relies on a one-time JS `RNRestart`. Phase 1 verdict: **fix natively** — restart-LOOP risk is low (RNRestart reloads in-process, so the just-written SharedPreferences value survives into the next isRTL read), but every fresh install on an LTR-locale Android still gets an LTR first frame + a visible restart, and a force-kill between `forceRTL()` and the pref flush repeats it. Exact ready-to-apply `MainApplication.kt` patch (RN 0.85 `I18nUtil.instance` API, verified against node_modules source) is in `audit/reports/phase-1.md` §3 — it could not be applied from this session because `android/`/`ios/` are **gitignored + untracked** and live only in the main checkout (the audit worktree has no copy and is isolation-blocked from editing it). Owner applies it like the AppDelegate.swift fix was applied. **Merge-day update (2026-07-16): the patch IS applied in the main checkout** (`I18nUtil` present in MainApplication.kt) — but `/android` is gitignored, so the fix lives ONLY on this machine: a fresh clone/prebuild silently loses it. Phase 13 must verify the shipped artifact contains it; consider tracking the customized native files (or a config plugin) — logged as part of the release-risk register. Note: AppDelegate.swift:24-26's comment claims Android already enforces this natively — false until the patch lands. | fixed (local, **untracked**) | — |
| F-011 | 0→4 | src/components/section/SectionNavBar.tsx:59 | P3 | `/* TODO: search */` — section nav search affordance was a no-op stub (predated the search feature). Phase 4: wired to the بحث screen from every section page + the attachment reader. | fixed | — |
| F-012 | 0→10 | src/api/admin.ts:80 | P3 | Admin lectures fetch guarded at `.limit(1000)` with an explicit "pagination TODO" once the library grows. Phase 10 admin sweep. | open | — |
| F-013 | 0→2,3 | (live auth) | **P1 lead** | Seeded demo accounts (`admin@`/`user@`/`publisher@`/`sheikh@gmail.com`) — owner consciously deferred cleanup (GLITCH_LOG #13). The seed *scripts* were de-hardcoded, but the literal password is **still readable in 8 committed docs** (PLAN.md §3, PLAN_SECURITY.md, PLAN_V8, 5 PROMPT files) and in git history — treat it as permanently burned: any account still using it must be deleted or re-passworded, not "cleaned up in docs". GLITCH #15: `admin@gmail.com` no longer authenticates (unexplained). Verify actual account states live and close this out. | open | — |
| F-014 | 0→13 | (Supabase settings) | P3 | Leaked-password (HIBP) protection unavailable on the current Supabase plan tier (402 on enable — GLITCH_LOG #8). Min length 8 is enforced. Owner: upgrade plan or consciously accept; record in the sign-off risk register. | open | — |
| F-015 | 0→2 | supabase/migrations | P2 | Replay-on-empty-DB now verified via the F-002 staging build: **89 of 90 migrations replay clean; 0069 does NOT** — it `create or replace`s `get_featured_lectures()` with a changed return type (adds `audio_size_bytes`), which Postgres rejects (42P13) without a prior `DROP FUNCTION`; production must have received a manual console DROP (the exact drift GLITCH_LOG #10 implied). Staging was repaired with `drop function if exists public.get_featured_lectures();` then 0069 applied. Remaining decision for Phase 2 merge: prepend that guarded drop to 0069 (no-op for DBs that already ran it, makes the chain self-replaying) vs. document the manual step. Staging's `schema_migrations` is backfilled 0001–0090; production's still lists only 0001–0006 — backfill it the same way before ever using `supabase db push` there. | fixed (staging) / decision open | — |
| F-016 | 0→2,5 | (content pipeline) | P2 lead | Admin upload accepts `.ogg`; expo-audio cannot decode Ogg Vorbis on iOS (accepted in PLAN_ADMIN_FIXES when Android-first — but iOS submission is now in scope). Any ogg lecture would be silent on iOS. Phase 2: inventory live audio formats; Phase 5: verify + decide transcode/restrict. | open | — |
| F-017 | 0→10 | app/admin/*, app/sheikh/* | P2 lead | V16 small-screen responsive audit covered the **student app only** — admin + sheikh surfaces were explicitly out of that pass and have never had one (admin IS used from phones per PLAN_ADMIN_V5). Phase 10 checklist covers it. | open | — |
| F-018 | 0→9 | supabase/functions | P3 | V12 item 6's email leg (Resend `notify-email` function for content reports) was never shipped — no such function exists. Deferred feature, needs owner's `RESEND_API_KEY` if still wanted. Not a defect; record so it isn't "rediscovered". | open | — |
| F-019 | 0→5,9,13 | IOS_SUBMISSION.md | **P1 lead** | The entire iOS device-check list is explicitly unverified ("Nothing in this pass was device-tested"): push e2e (token platform fix included), background/lock-screen audio, RTL first launch without LTR flash, safe areas incl. iPad, account-deletion wipe, icon rendering. Requires a physical iPhone (emulators lie for audio/push). | open | — |
| F-020 | 0→4 | app/(student)/sheikh-info.tsx | P3 | `DISPLAY_NAME_OVERRIDES` hardcoded an environment-specific UUID in the screen. Phase 4: moved to `src/config.ts` (`SHEIKH_DISPLAY_NAME_OVERRIDES`) and applied at every render site via one helper. Full closure is an **owner data action**: rename `sheikhs.name` in the production DB, then delete the config entry. | fixed (code) | — |
| F-021 | 0→13 | android/app/build.gradle | P3 lead | Per-ABI APK splits with `universalApk false`: direct-APK distribution (the landing page serves a single file) can hand the wrong ABI to a phone → "app not compatible" install failures (diagnosed, deliberately not changed in V14; store AAB path unaffected). Phase 13: make the distributed artifact strategy explicit (universal APK for direct download vs AAB for store). | open | — |
| F-022 | 0→2,11 | search_buddy_candidates (SQL) | P2 lead | Computes a per-candidate streak inside the search RPC — flagged as a potential hot query (PLAN_PERFORMANCE P6.2, never profiled). `EXPLAIN ANALYZE` on realistic volume in Phase 2/11. | open | — |
| F-023 | 0→9 | modules/floating-bubble, src/lib/bubble.ts | P3 lead | Floating bubble is experimental and OFF (`BUBBLE_ENABLED=false`, native module unlinked) yet `BubbleConsent` settings UI is mounted in profile.tsx. Phase 9: verify flag-off is truly inert end-to-end (incl. what the consent row does when the module is absent). | open | — |
| F-024 | 0→11 | (web) | P3 | Web boot logs deprecations from react-native-web 0.21: `"shadow*" style props are deprecated. Use "boxShadow"` and `props.pointerEvents is deprecated. Use style.pointerEvents`. Cosmetic today; will break on a future rn-web major. Sweep in Phase 11. | open | — |
| F-025 | 3 | src/lib/outboxQueue.ts, src/hooks/useAuth.ts | **P1** | Offline-outbox entries carry no user id and survived every identity change: pending private **note bodies** and listening ticks replayed under the NEXT session's uid — sign-out → guest, guest → freshly signed-in account, ban → guest, and even **after delete-account** (the "deleted" user's data reappeared under the guest identity; on a shared device it lands in another person's account). Fixed: `clearOutbox()` at every identity boundary (in `stopDeviceSideEffects` for sign-out/delete, in `useSignIn`, in the ban path) + a queue generation counter that aborts an in-flight flush's stale snapshot mid-loop (security-review follow-up). Contract: identity changes discard un-synced offline writes. | fixed | — |
| F-026 | 3→2 | app/(auth)/register.tsx | **P1** | `/register` was reachable for already-registered sessions (deep link `riwaqalilm://register`, typed web URL, in-app history — AuthGate deliberately keeps `(auth)` open for guests): re-running register() `updateUser`s a NEW name/gender over the oath-locked identity (edit-profile locks both in UI; gender drives رفيق الدرب segregation) and silently resets the password without knowing the current one. Fixed client-side: `<Redirect href="/">` for non-guest sessions (inert while a register mutation is pending/succeeded). **Phase 2 lead stays open**: `set_own_profile` / `updateUser` must enforce name/gender immutability server-side — the client guard is UX, not security. | fixed (client) | — |
| F-027 | 3 | src/hooks/useAuth.ts, app/_layout.tsx | P2 | Query cache was never reset on **sign-in** or on **ban-forced sign-out** (only on voluntary sign-out): the previous identity's Home resume card, section progress %, notes and journey caches kept rendering — and persisting to disk — under the new identity for up to the 30-min staleTime. Fixed: `useSignIn` now clears outbox + query cache and primes the fresh user inside mutationFn (mirrors useSignOut); ban path now runs `forcedSignOutCleanup` (audio stop + local-notification cancel + outbox clear + cache reset) instead of a bare currentUser flip. | fixed | — |
| F-028 | 3 | app/(auth)/reset-password.tsx | P2 | OTP reset flow: client accepted 6–7-char passwords but the server enforces min 8 — `verifyOtp` **consumed the single-use recovery code**, then `updatePassword` failed, and every retry re-verified the now-dead code («الرمز غير صحيح» for a code that was correct); user forced to resend. Fixed: min-8 client gate + `verified` state (retry skips straight to the password write against the live recovery session; resend resets it). Also: done-redirect `setTimeout` now cleaned up on unmount (no more navigation yank after the user left the screen). | fixed | — |
| F-029 | 3 | app/(auth)/register.tsx, app/(student)/edit-profile.tsx | P2 | Same min-6-vs-server-8 mismatch on register (placeholder already said 8; server rejection surfaced raw) and change-password (rejection landed after the current-password re-auth had already succeeded). Fixed: min-8 gates, inline Arabic too-short error on register, «٨ أحرف» placeholder on edit-profile. | fixed | — |
| F-030 | 3 | app/(auth)/register.tsx, src/lib/authErrors.ts | P2 | Register screen rendered raw English Supabase errors («User already registered» on an Arabic-first form); sign-in/reset/edit-profile each kept a partial private mapper whose fallback leaked unmapped English verbatim. Fixed: shared `arabicAuthError()` (Arabic-text passthrough for already-Arabic messages like the auth-timeout error, calm generic Arabic fallback) now used by all four surfaces. | fixed | — |
| F-031 | 3 | app/(student)/edit-profile.tsx | P2 | Only auth form without a KeyboardAvoidingView on this edge-to-edge app (keyboard overlays instead of resizing): the password section — three fields + save button, the tallest and lowest row — sat under the keyboard with no scroll escape (the exact failure mode documented and fixed on register/sign-in previously). Fixed: same `behavior="padding"` wrapper. | fixed | — |
| F-032 | 3 | app/(auth)/register.tsx, app/(student)/edit-profile.tsx | P3 | Polish batch, fixed in-phase: Latin "6" instead of `arNum` in edit-profile's OTP copy; oath modal's `onRequestClose={() => {}}` made Android hardware-back a dead key inside the modal (now mirrors «رجوع»). | fixed | — |
| F-033 | 3→2,9 | (server) | P2 lead | Ban enforcement cannot unregister the device push token (the banned JWT's requests are rejected before the delete runs), so the banned account's tokens stay live in `push_tokens` — the server push fan-out must exclude banned users or their device keeps receiving pushes. Verify in Phase 2 (cron/RPC queries) and Phase 9 (e2e). | open | — |
| F-034 | 3 | src/api/auth.ts (register) | P3 | The optional-email second step of register() swallows failures by design (registration must never fail on the optional field) — user gets no notice the email wasn't saved (profile shows «—», and password recovery by email silently unavailable). Deliberate tradeoff; revisit copy in a polish phase (e.g. notice on profile when email is empty). | wontfix (documented) | — |
| F-035 | 3→11 | src/api/auth.ts (normalizePhone), register/edit-profile phone inputs | P3 | Phone edge cases: an 8-digit local input passes the ≥8-digit gate but `normalizePhone` prepends the country code producing an invalid-length E.164 silently (sign-in with the same typo works, so self-consistent but unrecoverable for recovery flows); Arabic-Indic digits (٠-٩, e.g. pasted from WhatsApp) are stripped by the `[^0-9]` filters so the submit button stays disabled with no explanation. Log for the Phase 11 polish batch: stricter length gate + Arabic-digit normalization. | open | — |
| F-036 | 4→2 | src/api/lectures.ts, supabase (lectures RLS, get_featured_lectures) | **P1** | **Gender-visibility leak on browse lists.** `lectures_select` RLS checks only `status='published'` — gender scoping lives solely inside specific RPCs — and 0049's own comment flags `get_featured_lectures()` as the one list it left unfiltered. Result: «أحدث الدروس» (raw table select) and «المختارات» (rail + full screen, via `get_home_page`'s featured leg AND the standalone RPC) showed — and played — lectures from opposite-gender sections that Home's other rails correctly hide. **Interim client fix shipped**: `filterVisibleLectures()` passes these small lists through the existing `lecture_visible_to_viewer` RPC (parallel, fail-open); recent over-fetches 3× then slices so the filter can't leave the list short. **Server root cause stays open → Phase 2**: scope `get_featured_lectures` + add a filtered recent RPC (or extend lectures RLS), then delete the client filter (it costs Home's featured leg +1 RTT). | fixed (client interim) | — |
| F-037 | 4 | src/components/home/ContinueCard.tsx | P2 | The card's pause-glyph button never paused — it navigated to the player regardless, contradicting its own «إيقاف مؤقت» accessibility label. Fixed: pause when active; resume via `playLecture` when current-but-paused (`preloadLecture` deliberately no-ops on the current lecture, so the old path could navigate with audio staying paused); `stopPropagation` so the web build doesn't bubble the press into the card's navigate. | fixed | — |
| F-038 | 4 | app/(student)/sheikh-info.tsx | P3 | Sheikh photo (signed R2 URL that can outlive its TTL in the 30-min/persisted cache) rendered an empty circle on load failure. Fixed: `onError` falls back to the placeholder icon, reset when a refetch mints a fresh URL. | fixed | — |
| F-039 | 4→5 | src/components/section/LectureRowItem.tsx | P2 lead | Every mounted lecture row fires `prefetchPlayback` (a signed-URL mint) — scrolling a 500-lecture section issues hundreds of r2-read-url calls (each row is a distinct cache key, so query-cache dedupe doesn't bound it). Deliberate tap-latency tradeoff; Phase 5 (owns audioController) should bound it (e.g. viewport-visible rows only, or an LRU/mint budget). | open | — |
| F-040 | 4→2 | src/api/lectures.ts (getRecentLectures), app/(student)/recent.tsx | P3 | «عرض الكل» behind أُضيف حديثاً shows the same 8 items as the rail itself (both capped at 8) — the full-list screen adds status/download affordances but no additional history. Fold into the Phase 2 recent-RPC fix (parameterized limit, e.g. 30, filtered server-side). | open | — |
| F-041 | 4→11 | app/(student)/search.tsx, app/attachment/[id].tsx | P3 | Polish batch: search's sheikh results all navigate to the single sheikh-info page with no id (tapping the 2nd matched sheikh opens a page headlined by the 1st); huge transcripts render as ONE Text node (no chunking — slow layout on very large tafrīgh); section/attachment "غير موجود" empty-states also show for plain network errors (no offline wording). | open | — |
| F-042 | 4 | (verification) | P2 lead | Phase 4 exit criterion "tree edge cases seeded in staging and verified" is **waived pending F-002** (no staging; seeding pathological trees into production is prohibited). Deep-tree (5+ levels), 500-lecture, only-drafts, and cycle cases were verified by reading `get_section_page`/rollup SQL semantics + the FlatList section page, not live. Re-run against staging when it exists (queue with the Phase 2 replay test). | open | — |
| F-043 | 6 | src/api/progress.ts, src/api/journey.ts, supabase/migrations/0090 | **P1** | **Streak day-attribution split-brain (device-local vs server-UTC).** The live save called `save_activity` WITHOUT `p_day` → credited the server's UTC day, while the offline outbox replays with the DEVICE-LOCAL day (its documented contract). For the target audience (UTC+2…+4) local midnight is 2–4h before UTC midnight, so in that window the same session's listening landed on different days depending on connectivity — a session straddling a connectivity blip could split its minutes across two rows leaving BOTH under the 120s meaningful bar → streak break despite real listening. All read rollups (`today_counted`, streak anchor, Sat→Fri week bounds) also anchored on UTC `current_date`, so «واصلت اليوم» misreported around local midnight. Fixed: live path now passes `p_day: localDay()`; migration **0090** (authored, **NOT applied** — F-002 pattern) day-parameterises the read rollups (`p_today`, clamped ±2d) and clamps future `p_day` writes to ≤ current_date+1; client reads pass `p_today` with a PGRST202 fallback (verified live: pre-0090 prod returns PGRST202 → fallback engages), so either order of {migration applied, app shipped} stays correct. **Owner action: apply 0090 via management API before shipping this build, then regenerate database.generated.ts and drop the fallback.** | fixed (client) / migration authored | — |
| F-044 | 6 | src/components/journey/StreakRing.tsx | P3 | «أطول مداومة: ٢ يوماً» — wrong Arabic dual/plural for 2 and 3–10 (should be يومان / أيام); `arDayCount` existed and is used by every other streak surface. Fixed: uses `arDayCount`. (The ring's inner caption «يوم مداومة» under the big numeral is a deliberate two-line design — left as-is.) | fixed | — |
| F-045 | 6 | src/components/journey/BuddyCompareCard.tsx | P3 | Dead ternary `fem ? 'أنت ورفقاؤك' : 'أنت ورفقاؤك'` (both branches identical — the female wording was evidently intended and lost) + four phrases addressed masculine to female users («فاثبت وواصل», «أنت متقدم بخطوة», «كلاكما أكمل هدفه», «كلاكما مستمر») in a strictly gender-segregated feature. Fixed: full feminine forms on the female side (أنتِ ورفيقاتك، فاثبتي وواصلي، كلتاكما…). | fixed | — |
| F-046 | 6→11 | supabase (weekly_goals), src/lib/outbox.ts | P3 | `weekly_goals.target` has no CHECK constraint — any integer (0, negative, 10⁶) lands via direct PostgREST upsert; the client UI only offers presets so this is API-surface hygiene, and the rollups degrade gracefully (target 0 → ratio 0, never reached, no div-by-zero anywhere incl. `try_claim_goal_congrats` which explicitly guards target=0). Also: an offline goal edit replays last-write-wins over a newer edit from a second device (no updated_at guard). Batch a CHECK (target between 1 and 10080) into the next owner-applied migration; two-device goal conflict logged as accepted (single-value preference, low stakes). | open | — |
| F-047 | 6→11 | src/components/journey/GoalEditorSheet.tsx | P3 | The re-seed effect runs on every `initial.metric/target` change even while the sheet is OPEN — a background refetch landing mid-edit stomps the user's un-saved metric/target selection. Narrow (requires a server-side goal change while the sheet is open); fix is to seed only on the closed→open transition. Log for the Phase 11 polish batch. | open | — |
| F-048 | 6 | supabase/migrations/0014/0044/0046 (recovery placeholders) | P3 | Streak-recovery bridge rows (1-second placeholder days) count into «أيام النشاط» (active_days) and totalSeconds — a recovered streak inflates the lifetime active-day stat by ≤2 days per recovery. Invisible-noise level; recording as wontfix (the placeholder-day design is deliberate and the stat stays "days the app considers you active", which a recovered gap arguably is). | wontfix (documented) | — |
| F-049 | 6→13 | src/api/journey.ts (rpcWithLocalToday), src/types/database.generated.ts | P3 | **Post-0090 cleanup tracker** (deliberately its own row so the transitional shim can't outlive its purpose): once 0090 is applied, (1) regenerate `database.generated.ts`, (2) delete `rpcWithLocalToday` + its `as never` casts in favor of direct typed `{ p_today }` calls (journey summary, streak status, goal congrats). Until then the shim memoizes the PGRST202 probe (one wasted round-trip per session, not per read). Risk if skipped: a future SQL rename of `p_today` would compile clean and silently revert reads to server-UTC days via the fallback — F-043 resurrected undetectably. | open | — |
| F-050 | 6→11 | supabase/migrations/0082 (get_buddies_compare), src/components/journey/BuddyCompareCard.tsx | P3 | After 0090, the buddy-compare card's TWO sides use different day anchors: «أنت» values shown next to it (journey summary/streak, local-day) vs the 0082 buddy rollup (server-UTC day, including the viewer's own leg inside the compare RPC). In the local-midnight→UTC-midnight window (2–4h/day; weekly at the Sat rollover) the phrases can misreport — e.g. «رفيقتك أكملت هدفها… وواصلي» purely from the anchor mismatch, a false "you're behind" in a calm-by-design product. Fix direction: parameterize the viewer's own leg of `get_buddies_compare` with p_today in the next owner-applied migration batch (buddy-side legs stay server-day — another user's local day is unknowable). | open | — |
| F-051 | 8 | app/(student)/quiz-attempt/[attemptId].tsx | **P1** | **Timed-quiz countdown froze across backgrounding.** The countdown decremented a counter via chained 1s timeouts — RN freezes JS timers in background, so after a phone call / app switch the timer showed phantom time, never reached zero (the ONLY auto-submit trigger), and every answer past the real deadline was refused server-side while the UI showed a misleading «تحقق من الاتصال» banner; the user could "answer" for many minutes with everything silently dropped. Server integrity was never at risk (deadline+30s clamp, grading only in-time saves — verified in 0017) — this was pure client-state corruption on a completely ordinary path. Fixed: wall-clock deadline (`deadlineRef` seeded from the server's `remainingSec`), recomputed every tick AND on AppState→active; auto-submit fires on the first foreground tick past zero and retries if the submit itself fails. | fixed | — |
| F-052 | 8 | app/(student)/quiz-attempt/[attemptId].tsx, quiz-result/[attemptId].tsx | P2 | Both screens rendered `isLoading \|\| !data → spinner` with no error branch: a failed load — foreign/bogus attempt id via deep link (server raises «المحاولة غير موجودة»), a not-yet-submitted attempt opened as a result, or a plain network failure on first open — spun forever with no exit. Fixed: calm Arabic error state + «العودة» on both screens. | fixed | — |
| F-053 | 8 | app/(student)/quiz-attempt/[attemptId].tsx | P2 | A failed submit was **completely silent**: `onError` only reset the double-submit guard; the confirm sheet had already closed and the button returned to idle — an offline user reasonably believed they had submitted (and the attempt looks submitted until the next visit). Fixed: inline danger banner (server's Arabic reason verbatim, generic connectivity line otherwise); retry stays open; auto-submit retries on subsequent ticks. | fixed | — |
| F-054 | 8 | app/(student)/quiz/[id].tsx, quiz-attempt/[attemptId].tsx, src/lib/errorText.ts | P3 | English leakage + wrong blame in quiz error copy: `startError` rendered `err.message` raw (a two-device simultaneous-start `unique_violation` race or network error surfaces English PostgREST text on an Arabic-first screen); the save-failure banner always claimed a connectivity problem even when the server's real (Arabic) reason was «انتهى وقت الاختبار»; the intro's `!quiz` state blamed the quiz («الاختبار غير متاح») for what could be a dead connection. Fixed: shared `arabicOr()` (Arabic server messages pass through verbatim, everything else falls back to calm Arabic), network-vs-missing split on the intro empty state. | fixed | — |
| F-055 | 8 | supabase/migrations/0017 (quiz_result_payload), quiz-result screen | P3 | Switch combination `show_result=false` + `show_correct_answers=true` still renders the full per-question review (correct/incorrect icons included), which lets a student count checkmarks and reconstruct the hidden score. The payload honors each switch literally (verified); whether this combination is a leak or a legitimate "teach the answers, hide the grade" choice is an admin-semantics question. Documented for the Phase 10 quiz-editor pass (consider disabling the combination in the editor UI or hiding per-question isCorrect when show_result=false). | open | — |
| F-056 | 7 | supabase/migrations/0028 (questions_select RLS) → 0091 | **P1** | **Q&A anonymity bypass at the network layer.** The `questions_select_own_or_moderator` RLS policy (0028) granted moderators a RAW select over `public.questions` — including `asker_id` + `is_anonymous`. All legitimate moderator reads go through the DEFINER RPC `get_question_inbox`, which returns `'سائل'`/`null` for anonymous askers and ships `asker_id` to admins only (0077); the raw-table branch bypassed that entirely. **Confirmed live on staging:** a **sheikh**'s `from('questions').select('asker_id, is_anonymous')` returned the real `asker_id` of an anonymous question whose inbox row correctly showed `'سائل'`/`null`. Because `asker_id` is a stable per-user UUID, a sheikh can correlate an anonymous question with any NAMED question by the same asker (name revealed by the inbox/public list) and deanonymise — leaking to the exact party anonymity is meant to shield the asker from. **Fixed: migration 0091** drops the moderator branch → `questions_select_own` (own rows only); all moderator access is via RLS-exempt DEFINER RPCs and no client does a raw `from('questions')` select (verified repo-wide). Re-verified live: sheikh raw select → 0 rows; inbox/answer/public paths unchanged. Regression: `tests/contract` F-056. **Applied to production 2026-07-16** via the Management API (policy verified: `questions_select_own`, moderator branch gone); also on staging. NOT recorded in prod `supabase_migrations.schema_migrations` — same out-of-band drift as 0059+ (F-002/F-015 owner backfill task). | fixed | `fe42925` |
| F-057 | 7 | supabase/migrations/0078 (update_own_question) → 0092, src/hooks/useQuestions.ts | P2 | **Editing a question leaves a stale answer under the new text.** 0078's `update_own_question` resets a question to pending and clears the mirror answer columns on a body change (its comment: «a stale answer must never sit under new text»), but 0086 moved answers into `question_answers` and 0078 was never updated — so a body edit cleared the mirror yet left the answer-thread rows. `get_question_answers` reads that table, so re-answering the edited question shows the OLD answer ABOVE the new one (and for a sheikh-audience question can resurface a private prior answer under unrelated new text). **Confirmed live:** edit body → re-answer → thread = `[old, new]`. **Fixed: migration 0092** also deletes `question_answers` + clears `answer_audio_path` on a body change; **client** (`useQuestions.ts`) now invalidates the whole `['questions']` root so the cached answer thread is dropped too. Re-verified live: thread = `[new]` only. Regressions: `tests/contract` F-057 + `src/hooks/__tests__/useUpdateOwnQuestion`. **Applied to production 2026-07-16** via the Management API (function body verified: clears `question_answers` + `answer_audio_path` on body change); also on staging. NOT in prod `schema_migrations` (out-of-band drift, F-002/F-015). | fixed | `fe42925` |
| F-058 | 7 | supabase/migrations/0081 (lecture_benefits SELECT + admin_list_benefits) | P3 | Staff (sheikh) can deanonymise **benefit** authors: the `own_or_staff_viewer` SELECT policy exposes `lecture_benefits.user_id` on a raw select, and `admin_list_benefits` resolves `author_id`/`author_name` to sheikhs (email nulled for non-admin staff). Benefits are promised «تُنشر دون اسمك دائماً» to users, but the design deliberately resolves the author for moderation accountability and 0081 widened this from admin to sheikh (Phase-2-reviewed). Unlike F-056 this is NOT a network-layer regression — the RPC path already sanctions staff visibility. Logged as a **product decision**: should benefit authorship be visible to sheikhs, or admins only? No code change pending the ruling. | open (product decision) | — |
| F-101 | 1 | src/hooks/useAuth.ts:59, app/_layout.tsx:214 | **P1** | Offline cold start on a fresh install hangs on the boot loader FOREVER: `useEnsureSession` used TanStack's default `networkMode: 'online'`, so once `initConnectivity` seeds `onlineManager` offline the anon sign-in mutation is **paused, not run** — `ensure.isError` never turns true and SessionGate's documented fall-through (`!!user \|\| ensure.isError`) never fires. And in the race where the mutation did run and fail, nothing ever retried it: the app rendered session-less, every RLS-gated read (`to authenticated`) returned empty rows cached as *successes*, and Home stayed blank even after the network returned, until a force-restart. Fixed: `networkMode: 'always'` (local session checks work offline, the anon sign-in fails fast) + SessionGate retries on each offline→online edge and invalidates all queries once the late session lands. Hardened after code-review (`2425072`): single-flight `ensureSession` (no concurrent anon double-mint) + stable-dep reconnect effect with a live-cache re-check so a real sign-in completed meanwhile is never clobbered. | fixed | `759baec`, `2425072` |
| F-102 | 1 | app/_layout.tsx:579 | **P1** | No ErrorBoundary existed anywhere in the app (repo-wide grep: zero `ErrorBoundary`/`componentDidCatch`). Any unhandled render/lifecycle throw in any screen → RN fatal (app hard-closes to the launcher) in a native release build, blank page on web, with no recovery path. Fixed: root `ErrorBoundary` export in `app/_layout.tsx` (expo-router `Try` wrapper — verified wiring in expo-router `useScreens.js:146-155`): calm Arabic screen + إعادة المحاولة retry; self-contained (no providers/custom fonts, which may not exist at crash time). | fixed | `759baec` |
| F-103 | 1 | app/_layout.tsx:647 | P2 | `useFonts`' error result was discarded — a failed font load left `fontsLoaded` false forever and the app parked on the BootLoader with no timeout. Realistic path: the web admin dashboard fetches the 6 font assets over the network; one failed/hung fetch on a flaky connection bricked the whole dashboard. Fixed: `fontsLoaded \|\| !!fontError` gates SessionGate (system-font fallback instead of an eternal loader). | fixed | `759baec` |
| F-104 | 1 | app/+not-found.tsx | P3 | No `+not-found` route: a malformed external `riwaqalilm://…` link, stale notification payload, or mistyped web URL landed on expo-router's built-in **English** "Unmatched Route" screen (Arabic-first violation, dev-styled). Fixed: calm Arabic screen with a العودة إلى الرئيسية action. | fixed | `759baec` |
| F-105 | 1→3 | src/hooks/useAuth.ts:164-217 | P2 | Sign-out / delete-account are unusable offline: both mutations use the default `networkMode: 'online'`, so an offline tap on تسجيل الخروج pauses silently (spinner/no-op until the network returns) — even though `signOut()` is deliberately built to always succeed locally. Additionally `await unregisterPushToken(deviceToken)` in useSignOut's mutationFn is unguarded — a network error there rejects the whole sign-out before the local fallback is ever reached. Fix in Phase 3 (auth client): `networkMode: 'always'` + try/catch the token unregister (accepting the stale-token tradeoff offline). | open | — |
| F-106 | 1→3 | src/hooks/useAuth.ts:46-52, src/api/auth.ts:227-257 | P3 | Server-side role change mid-session is invisible until an app restart: `currentUser` has `staleTime: Infinity` and its role comes from the *stored JWT's* `user_metadata` via `getSession()` (no server read). Ban/delete IS caught within seconds (`checkBannedAndSignOut` on every foreground, _layout.tsx:439), but promotion/demotion isn't — a demoted admin keeps seeing the admin UI (RLS still blocks the data server-side; verified posture, cosmetic exposure only). Accepted for now; Phase 3 may piggyback a role refresh on the existing foreground `getUser()` call, which already fetches the fresh user and discards it. | open | — |
| F-107 | 1→2 | app/_layout.tsx:396, src/api/lectures.ts:181 | P3 | Gender-visibility guard (`isLectureVisibleToViewer`, 0072) runs on exactly two entry points — push-tap deep link (_layout.tsx:396) and the in-app inbox (notifications.tsx:138) — per the owner's 0072 decision. Every other path to `/player/[id]` is unguarded and `lectures` reads have **no server-side gender RLS**: a hand-crafted `riwaqalilm://player/<id>` scheme link opens a women's-section lecture for any viewer. No in-app surface mints lecture links (share shares only the app URL), so exposure needs a hand-built link today. Revisit in Phase 2: decide whether gender scoping stays courtesy-level or moves into RLS/RPC. | open | — |
| F-108 | 1→9 | app/_layout.tsx:410-421, src/lib/notifications.ts:560 | P2 lead | Cold-start deep link reads `getLastNotificationResponseAsync()` with no consumed-marker: on Android, relaunching the task from **recents** can redeliver the original launch intent, making the app re-open the last-tapped lecture on later cold starts that had nothing to do with the push. Also a remount of NotificationsBootstrap re-consumes it (rare: only after an UpdateGate hard-block flip). Needs physical-device verification (emulator recents behavior differs) — Phase 9's NotificationsBootstrap deep audit; fix would be a stored last-consumed response identifier. | open | — |
| F-109 | 1→13 | app/_layout.tsx:92-118 | P3 | Persisted-cache privacy posture (documented, deliberate): private lesson notes (`notes`), the notifications inbox, journey/streak snapshots, and quiz `myStats` are written **plaintext** to AsyncStorage (app-sandboxed but unencrypted; readable on a rooted/jailbroken device or from an `allowBackup` extraction). Allowlist itself is correctly built (deny-by-default roots, `admin` defence-in-depth, quizzes scoped to `myStats` only, errors never persisted, `buster: APP_VERSION`). Accepted-risk per V10/V11 design («حتى الملاحظات» offline is the product goal); record in the Phase 13 sign-off risk register. | open | — |
| F-110 | 1→4 | src/components/navigation/BottomNavBar.tsx:49-52 | P3 | `activeIndexFor` falls back to index 0 when the pathname is no tab: the الرئيسية pill renders as active (gold highlight) on every non-tab student screen (section pages, downloads, about, quiz…), where the bar is still visible. Misleading location signal; cosmetic. Phase 4 sweep. | open | — |
| F-111 | 1→10 | app/_layout.tsx:295-306, app/(auth)/sign-in.tsx:50 | P3 | A **student** signing in on web succeeds, then bounces `/' → /sign-in` (AuthGate: non-staff on web must be `inAuth`) with no explanation — the form just reappears signed-in-but-stranded. Web is staff-only by design; add a quiet Arabic notice ("التطبيق للطلاب — لوحة الويب للمشرفين") or a role-aware message on the web sign-in screen. Phase 10 (admin/web surface). | open | — |
| F-112 | 1→11 | src/components/UpdateGate.tsx:34 | P3 | `installedVersion` re-derives `Constants.expoConfig?.version ?? '0.0.0'` instead of importing the identical `APP_VERSION` (src/lib/version.ts) it sits next to (`compareVersions` IS imported from there). Duplication only — same value; batch into Phase 11 cleanup. | open | — |
| F-201 | 2 | supabase/migrations/0082_multi_buddy.sql | **P1** | Buddy anonymous-guard regression. 0041 (Security S3) blocked guests from رفيق الدرب: `send_buddy_request`/`respond_buddy_request` raise on `auth.jwt()->>'is_anonymous'`, and `search_buddy_candidates` excludes anon accounts. 0082 (multi-buddy) recreated all three via create-or-replace and reproduced the pre-0041 bodies **without those guards** — so since 0082 a guest who sets gender+name via `set_own_profile` reappears in buddy search and can send/accept invites, forming ghost pairs with real students. Confirmed by static replay (0082 bodies contain no `is_anonymous` check). Fixed: new migration reproduces 0082's ≤3-cap bodies verbatim + re-adds the 0041 guards. | fixed | `0088 (branch audit/phase-2-backend)` |
| F-202 | 2 | supabase/migrations/0038,0069 get_featured_lectures | **P1** | Featured (مختارات) rail bypasses gender-section visibility. 0049 gender-filtered every browse RPC via `section_visible_to_viewer` but its own header flags `get_featured_lectures` as unreviewed; it was never closed. `get_home_page` pulls `featured` straight from the unfiltered `get_featured_lectures`, so a lecture curated from a رجال/نساء-only section shows on the OTHER gender's Home rail — and its audio is then served (`can_read_storage_object` gates lecture objects on published status only, never gender). Featured is the practical discovery path for that key (all other reads are filtered). Fixed: new migration adds the `section_visible_to_viewer` predicate (content-manager bypass + unclassified passthrough preserved). | fixed | `0089 (branch audit/phase-2-backend)` |
| F-203 | 2 | supabase/functions/admin-users/index.ts | P3 | `createUser`/`setPassword` accept 6-char passwords while Supabase Auth min length is 8 — a valid-per-form 6–7-char password fails with an opaque GoTrue error. Align to 8. | open | — |
| F-204 | 2 | report_content / submit_feedback (SQL) | P2 | No rate-limit on guest-writable fan-out RPCs. `submit_feedback` has NO dedup/cap and each call fans a `feedback_received` notification (+push) to EVERY admin; `report_content` dedups per `auth.uid()` but a guest can churn fresh anon sessions. A script floods the admin inbox + push pipeline. Needs a per-user/day cap or throttle table. | open | — |
| F-205 | 2 | can_read_storage_object (SQL 0063/0084/0086) | P2 | Lecture-object read gate checks `status='published'` only, never gender. A leaked/guessed `audio_path` of a رجال/نساء-only lecture is signed by `r2-read-url` for the other gender. F-202 closes the practical discovery path; this is the residual defense-in-depth gap (add gender to the `lectures/%` branch). | open | — |
| F-206 | 2 | supabase/migrations/0052_blocked_words.sql | P2 | Blocked-word filter bypassable: `contains_blocked_word` strips tashkeel+tatweel but NOT zero-width chars (U+200C/D, U+200B, U+FEFF); whole-word `\m…\M` matching is evaded by concatenation/gluing; letter-variant coverage is hand-seeded only. Post-hoc moderation (reports+hide) still applies, so P2. List is migration-only (no admin UI). | open | — |
| F-207 | 2 | buddy_of/buddies_of/buddy_count (SQL) | P3 | These DEFINER helpers are granted to `authenticated` and take an arbitrary user id → any user can enumerate any other user's buddy UUIDs and pair count (buddy-graph leak; opaque UUIDs, no names/PII). | open | — |
| F-208 | 2 | get_section_rollup/get_children_rollups (SQL) | P2 | Rollup counts walk the raw subtree with no `section_visible_to_viewer` filter (0049 left them out), so a parent section's "X دروس" header can include lectures from a gender-hidden child subtree — count-only leak (titles/audio stay hidden). | open | — |
| F-209 | 2 | supabase/migrations/0068 web_prefix_tsquery | P3 | The only function defined without `set search_path` (0042 fixed this class for set_updated_at; 0068 reintroduced one). INVOKER + built-ins only, low risk, but trips the Supabase `function_search_path_mutable` advisor. | open | — |
| F-210 | 2 | supabase/functions/notify-on-publish + 0009 | P3 | Push webhook (0009) hardcodes the public anon JWT as a bearer literal in SQL, and `notify-on-publish` performs no role/source check on its body — any caller with a valid JWT (incl. a guest) can POST a crafted notification row and send an attacker-controlled push (title/body/deep-link) to any registered device whose UUID they know. No data leak / no fan-out control, but push spoofing/phishing. Proper fix = shared webhook secret (env var + trigger header + function check); deploy BLOCKED. | open | — |
| F-211 | 2 | scripts/security-check.mjs | P2 | The RLS regression script reads the URL/keys from `.env` (= PRODUCTION per F-002) and CREATES then deletes real users + an anon session + a draft lecture + a notification row against it. It must never run until staging exists (it writes to prod). Also its `get_public_questions` probe passes wrong param names (`p_search`/`p_lecture_id` vs `p_scope`), so that check "passes" only because the call errors — false confidence. NOT RUN this phase (hard rule). | open | — |
| F-212 | 2 | recursive section CTEs (SQL) | P3 | None of the 6 recursive section CTEs carry a `CYCLE` clause or depth cap, and generic `sections.parent_id` UPDATE has no cycle guard (only `admin_reorder_sections` guards single-parent). A content-manager-created `parent_id` cycle would spin/error `get_section_rollup`/`get_home_page`/`get_sections_flat` etc. Robustness. | open | — |

## Notes

- **Staging readiness (2026-07-15, follow-up to F-002):** auth config copied from production
  (anonymous sign-ins, autoconfirms, secure email change, OTP length 6, **password min 8** —
  confirming the Phase 3 min-8 fixes); demo accounts seeded (`admin@`/`user@`/`publisher@`/
  sheikh, password in `.env.staging.local` — NOT the burned F-013 password); minimal content
  tree seeded (العقيدة → التوحيد with 2 published + 1 draft lectures, plus a female-only
  section for gender-visibility tests). `scripts/security-check.mjs` ran for the first time
  ever (was unsafe against production — F-211): **20/20 checks pass on staging**. Still absent
  on staging: edge functions + R2 secrets (deploy when a phase needs them), real audio files
  (audio_path values are dummies — playback tests still need production URLs or uploaded fixtures).

- **Cross-branch (2026-07-15):** the parallel Phase 2 session (`audit/phase-2-backend`) authored
  migration **0089** fixing the `get_featured_lectures` gender leak **server-side** — the same
  root cause as F-036's featured half. 0089 is **not applied to any DB yet**; Phase 4's client
  filter (`filterVisibleLectures`) is the live protection until it lands, then the client filter
  can be reduced to the recent list only (whose raw-select path 0089 does not cover — verify at
  merge). Phase 3/4 findings here use sequential IDs (F-025…F-042) while Phases 1/2 used
  F-1xx/F-2xx ranges — reconcile numbering when the branches merge.

- **Phase 7 (2026-07-16, `audit/phase-7-community`):** community/UGC audit. Anonymity was
  verified at the **network layer** (raw RPC payloads + every raw table read each JWT can
  reach), not just the UI — see `audit/reports/phase-7-anonymity-memo.md`. Found F-056 (P1,
  raw `questions` table leaked `asker_id` of anonymous questions to sheikhs → migration 0091)
  and F-057 (P2, edit-body left stale `question_answers` → migration 0092 + client
  invalidation); logged F-058 (benefit-author staff visibility — product decision). Both
  migrations applied to **staging** and verified; **prod application is an owner action**.
  Branch cut from `audit/phase-12-tests`, so it carries 0090 but NOT 0088/0089 (those are on
  `audit/phase-2-backend`); 0091/0092 have no functional dependency on 0088/0089 and are
  self-consistent on this branch — at merge all of 0088–0092 order cleanly. Buddy round-trip,
  gender/guest guards, and guest-report were exercised live; `security-check.mjs` 20/20 on
  staging. Findings use sequential IDs continuing F-025…F-055 (reconcile with the F-2xx
  Phase 2 range at merge, as already noted below).
- GLITCH_LOG items **#1–#7, #9, #12, #14 (web I18nManager crash → react-native-web patch), #16–#18 (informational), #20 (/downloads crash → fixed in V10)** were verified as resolved in the historical record and are not re-opened here; their fixes get re-exercised naturally by the per-phase checklists.
- Historical PLAN docs' "open questions" that were later superseded by shipped work (V3's dispatcher options, V4's five questions, etc.) are not logged individually — the shipped behavior is what Phases 3–10 audit.
- The `landing/` static site is a separate, gitignored project — out of app-audit scope except the F-021 distribution interaction.
